{
  "name": "storefront-webapp",
  "type": "web-application",
  "version": "0.1.0",
  "language": "TypeScript",
  "buildpack": "docker",
  "appPath": "storefront-webapp",
  "entrypoint": "deployment/web-application",
  "exposure": "internet",
  "stories": [
    1,
    2,
    3,
    4,
    5,
    6,
    7,
    8,
    9
  ],
  "dependencies": [
    {
      "description": "Calls the Ceramics API for the product catalog, cart, checkout, and the shopper's own order history.",
      "kind": "component",
      "name": "ceramics-api"
    },
    {
      "description": "Thunder-issued OIDC sign-in (PKCE) so a shopper can authenticate before browsing or checking out.",
      "kind": "platform-resource",
      "name": "user-auth",
      "parameters": {
        "scopes": "openid profile email"
      },
      "resourceType": "thunder-app"
    }
  ],
  "description": "The customer-facing storefront: a React SPA where a signed-in shopper browses the handmade-ceramics catalog, searches/filters products, manages a cart, checks out, and views their own order history. It holds no server-side state and no secrets — all catalog, cart, order, and payment logic lives behind ceramics-api. It does not expose any admin/catalog-management capability.",
  "skillsPinned": [
    "wireframes",
    "react-webapp",
    "thunder-authentication"
  ]
}
